(function () {
  /*
   Сделайте функцию, которая параметрами принимает 2 числа. Если их сумма больше
    10 - пусть функция вернет true, а если нет - false
   */
  function calcSumTask() {
    function calcSum(a, b) {
      return console.log(a + b >= 10);
    }

    // calcSum(4, 15);
  }

  window.task2 = calcSumTask;
}());
